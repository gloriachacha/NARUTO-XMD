const config = require('./config');
const utils = require('./utils');

const bot = async () => {
  // Bot logic goes here
  console.log('Bot is running!');
  // Use config and utils as needed
};

bot();